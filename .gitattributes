# Переводы строк нормализуются в репозитории в LF, в рабочей копии Windows
# оставляет свои. Без этого первый же коммит с другой машины показал бы
# «изменены все строки во всех файлах».
* text=auto eol=lf

# Бинарное — не трогать.
*.png binary
*.jpg binary
*.woff2 binary
*.ico binary
